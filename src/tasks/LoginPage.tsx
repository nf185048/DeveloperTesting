import React from 'react'
import type { FC } from 'react'

export const LoginPage: FC<{}> = () => {
  /**
   * TASK: Create a login interface.
   * 
   * Use the following components to create a login interface: Button, Card,
   * Grid, Input, InputWrapper, Typography, 
   */

  return (
    <>
      {/* Content */}
    </>
  )
}
